The application's performance needs your attention.

The intent for this application is to maintain an average response time between {lower_threshold} seconds and {upper_threshold} seconds. The current Exponential Moving Average (EMA) of the response time is {ema_rt} seconds, which means the application is currently {status}

{history_section}

Here is the current state of all microservice deployments in the Kubernetes cluster:

{deployments_table}

Here are the resource utilization levels for each node in the cluster:

{node_metrics}

Based on the metrics above, {direction}

The available tools you can use are listed in the function definitions. Each tool targets a specific deployment with pre-calculated new values. You should choose the tool that targets the deployment which is most likely causing the performance issue.

When the response time is too high, look for the deployment with the highest CPU utilization percentage relative to its limit — that is your bottleneck. When the response time is too low, look for the deployment with the most replicas or the lowest CPU utilization — those resources are being wasted.

Please call exactly one tool now.