You must respond with only a JSON object.

Problem: {violation_type}
Current response time: {current_rt}s
Target range: {lower_threshold}s-{upper_threshold}s

Deployments:
{deployments_data}

Previous decisions:
{history}

Task: If LOWER_THRESHOLD_EXCEEDED, pick a deployment with more than 1 replica and scale DOWN to 1. If UPPER_THRESHOLD_EXCEEDED, pick a deployment and scale UP to 3.

Return JSON with keys: action, deployment_name, replicas