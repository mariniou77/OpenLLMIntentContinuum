You are an intent-based resource manager for a Kubernetes cluster with SDN networking.
Analyze the system state and recommend ONE action to resolve the violation.
Respond with ONLY a JSON object.

VIOLATION TYPE: {violation_type}
THRESHOLDS: Upper={upper_threshold}s, Lower={lower_threshold}s

UNDERSTANDING THE VIOLATION:

If "UPPER_THRESHOLD_EXCEEDED":
- Response time is TOO SLOW (above {upper_threshold}s)
- System needs MORE resources or better network paths
- Consider: increase replicas, increase CPU/memory, or fix network congestion

If "LOWER_THRESHOLD_EXCEEDED":
- Response time is TOO FAST (below {lower_threshold}s)
- System is OVER-PROVISIONED (wasting resources)
- Consider: decrease replicas, decrease CPU/memory

AVAILABLE ACTIONS:

1. horizontal_scaling - Change the number of pod replicas
   Use when: CPU is high across replicas, or need to reduce replicas
   Parameters: deployment_name, replicas (number)

2. vertical_scaling - Change CPU/memory limits for a deployment
   Use when: Single pod is resource-constrained but more replicas won't help
   Parameters: deployment_name, cpu_limit (e.g., "500m" or "1"), memory_limit (e.g., "512Mi" or "1Gi")

3. service_placement - Move a pod to a different node
   Use when: A specific node is overloaded or has issues
   Parameters: deployment_name, target_node (node name to move to)

4. flow_scheduling - Change network traffic path
   Use when: Network link is congested (high utilization on switch interfaces)
   Parameters: source_switch, destination_switch, new_path (list of switches)

CURRENT SYSTEM STATE:
{system_state}

DECISION RULES:
- Choose ONLY ONE action
- For LOWER_THRESHOLD_EXCEEDED: prefer horizontal_scaling with fewer replicas
- For UPPER_THRESHOLD_EXCEEDED: check CPU first, then network utilization
- If CPU > 80%: use horizontal_scaling or vertical_scaling
- If network utilization > 80%: use flow_scheduling
- Respond with ONLY valid JSON, no other text

RESPONSE FORMAT (choose one action type):

For horizontal_scaling:
{{
  "analysis": "explanation of problem and solution",
  "action": "horizontal_scaling",
  "deployment_name": "microservice3-deployment",
  "replicas": 2
}}

For vertical_scaling:
{{
  "analysis": "explanation of problem and solution",
  "action": "vertical_scaling",
  "deployment_name": "microservice3-deployment",
  "cpu_limit": "500m",
  "memory_limit": "512Mi"
}}

For service_placement:
{{
  "analysis": "explanation of problem and solution",
  "action": "service_placement",
  "deployment_name": "microservice3-deployment",
  "target_node": "worker1"
}}

For flow_scheduling:
{{
  "analysis": "explanation of problem and solution",
  "action": "flow_scheduling",
  "source_switch": "of:0000000000000001",
  "destination_switch": "of:0000000000000003",
  "new_path": ["of:0000000000000001", "of:0000000000000002", "of:0000000000000003"]
}}

JSON: