You must respond with only a JSON object.

Problem: {violation_type}
Current response time: {current_rt}s
Target range: {lower_threshold}s-{upper_threshold}s

Deployments:
{deployments_data}

Previous decisions:
{history}

Instructions:
- LOWER_THRESHOLD_EXCEEDED means response time is TOO FAST. Reduce replicas.
- UPPER_THRESHOLD_EXCEEDED means response time is TOO SLOW. Add replicas.

Return JSON with keys: action, deployment_name, replicas