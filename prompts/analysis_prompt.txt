PROBLEM: EMA Response Time is {ema_rt}s (target: {lower_threshold}s-{upper_threshold}s)
{history_section}
LIMITS: {constraints}

STATE:
{deployments_table}

INSTRUCTIONS:
1. Find the deployment with the highest CPU utilization percentage to be the target.
2. Apply these rules to the target:
   - IF RT > {upper_threshold}s AND CPU > 80%: action="vertical_scaling", set new cpu_limit to (current + 200m), keep current memory_limit.
   - IF RT > {upper_threshold}s AND CPU <= 80%: action="horizontal_scaling", set new replicas to (current + 1).
   - IF RT < {lower_threshold}s AND replicas > 1: action="horizontal_scaling", set new replicas to (current - 1).
   - IF RT < {lower_threshold}s AND replicas == 1: action="vertical_scaling", set new cpu_limit to (current - 100m, min 100m), keep current memory_limit.

ALLOWED JSON FORMATS:
- {{"action": "vertical_scaling", "parameters": {{"deployment_name": "...", "cpu_limit": "...m", "memory_limit": "...Mi"}}}}
- {{"action": "horizontal_scaling", "parameters": {{"deployment_name": "...", "replicas": 0}}}}
- {{"action": "none", "parameters": {{}}}}

JSON: